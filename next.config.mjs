/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "i.ibb.co",
      "images.unsplash.com",
      "tiimg.tistatic.com",
       "cdn.vetverified.com",
    "upload.wikimedia.org",
    ],
  },
};

export default nextConfig;